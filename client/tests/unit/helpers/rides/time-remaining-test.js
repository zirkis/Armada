
import { ridesTimeRemaining } from 'client/helpers/rides/time-remaining';
import { module, test } from 'qunit';

module('Unit | Helper | rides/time remaining');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ridesTimeRemaining([42]);
  assert.ok(result);
});

