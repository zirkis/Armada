
import { ridesPourcentage } from 'client/helpers/rides/pourcentage';
import { module, test } from 'qunit';

module('Unit | Helper | rides/pourcentage');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ridesPourcentage([42]);
  assert.ok(result);
});

