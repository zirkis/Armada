
import { ridesDistanceRemaining } from 'client/helpers/rides/distance-remaining';
import { module, test } from 'qunit';

module('Unit | Helper | rides/distance remaining');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ridesDistanceRemaining([42]);
  assert.ok(result);
});

