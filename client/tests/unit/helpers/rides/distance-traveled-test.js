
import { ridesDistanceTraveled } from 'client/helpers/rides/distance-traveled';
import { module, test } from 'qunit';

module('Unit | Helper | rides/distance traveled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ridesDistanceTraveled([42]);
  assert.ok(result);
});

