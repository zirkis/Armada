
import { ridesDuration } from 'client/helpers/rides/duration';
import { module, test } from 'qunit';

module('Unit | Helper | rides/duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ridesDuration([42]);
  assert.ok(result);
});

