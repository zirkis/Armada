
import { unescape } from 'client/helpers/unescape';
import { module, test } from 'qunit';

module('Unit | Helper | unescape');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unescape([42]);
  assert.ok(result);
});

